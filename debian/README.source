In order to remove the JSON-licensed files from the HHVM project, I put together
a script called debian/repack that can be called with a git tag and will
generate a proper tarball (compressed with XZ) with git submodules, without the
.git artifacts and with the offending files removed.


 -- David Martínez Moreno <ender@debian.org>  Sat, 26 Jul 2014 23:35:30 -0700
