Description: Fix a syntax error in the HPHP grammar.
 If we try to regenerate the Bison lexer files, the build breaks due to a
 missing semicolon.
 .
 Ported from an internal diff, D1460649.
Author: Paul Tarjan <pt@fb.com>
Forwarded: not-needed
Reviewed-By: David Martínez Moreno <ender@debian.org>
Last-Update: 2014-07-27

--- hhvm-3.2.0+dfsg.orig/hphp/parser/hphp.y
+++ hhvm-3.2.0+dfsg/hphp/parser/hphp.y
@@ -1853,13 +1853,13 @@ lambda_expression:
 lambda_body:
     T_LAMBDA_ARROW expr               { $$ = _p->onExprForLambda($2);}
   | T_LAMBDA_ARROW
-    '{' inner_statement_list '}'      { $$ = $3;}
+    '{' inner_statement_list '}'      { $$ = $3; }
 ;
 
 shape_keyname:
     T_CONSTANT_ENCAPSED_STRING        { validate_shape_keyname($1, _p);
                                         _p->onScalar($$, T_CONSTANT_ENCAPSED_STRING, $1); }
-  | class_constant                    { $$ = $1 }
+  | class_constant                    { $$ = $1; }
 ;
 
 non_empty_shape_pair_list:
